# 2026-06-21 (domingo)

## 00:30 — Planejamento e fundação documental do Windows Intelligence Center
- feito:
  - escrito `docs/PLANEJAMENTO.md` — plano vivo do projeto: diagnóstico dos
    ativos, princípios, stack, estrutura-alvo, modelo SQLite e roadmap em fatias
    verticais (0–9), com detalhe das Fatias 0 e 1
  - escrito `CLAUDE.md` na raiz, orientando futuras sessões
  - criado este sistema de contexto (`.claude/contexto/`)
- decisões:
  - stack travada: **FastAPI + React (Vite)** no backend/frontend, **SQLite**
    (via camada de repositório, trocável) no armazenamento local
  - ponto de partida: **Fatia 0 (fundação) + Fatia 1 (métricas ao vivo)**
  - as duas pastas existentes são **referência**, não o produto:
    `Dash_Manager_Windows/` = fonte de lógica dos collectors;
    `dashboards_us-media/` = molde estrutural (descartar nuvem/multi-tenant)
  - produto é **local, single-user, privacidade-first**
- viradas:
  - a 1ª versão do `CLAUDE.md` tratava as pastas como "dois projetos do repo";
    corrigido para o enquadramento certo — **projeto greenfield + pastas de
    referência**, guiado pelo `PLANEJAMENTO.md`
- próximos:
  - resolver a higiene do `dashboards_us-media/` (tem `.git` próprio aninhado)
    antes de qualquer commit na raiz
  - implementar a Fatia 0 (esqueleto `backend/`+`frontend/`, `config`,
    `/api/health`, `scripts/start.ps1`, `.gitignore`)
- artefatos:
  - `docs/PLANEJAMENTO.md`
  - `CLAUDE.md`
  - `.claude/contexto/_FORMATO.md` + este arquivo
