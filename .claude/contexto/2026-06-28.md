# 2026-06-28 (domingo)

## 23:57 — Aplicação dos hooks do claude-harness no WIC + correção do molde na fonte
- feito:
  - aplicados os dois hooks do harness em `.claude/settings.local.json` (arquivo
    local, não versionado), preservando as 54 regras de `permissions`:
    - **PreToolUse** (`matcher: Bash|PowerShell`) — guard que ecoa lembrete antes
      de comandos destrutivos
    - **Stop** (`matcher: *`) — `command` que emite `systemMessage` lembrando de
      registrar nota em `.claude/contexto/`
  - JSON final validado com Python (não há `jq` na máquina): `permissions`
    intacto + bloco `hooks` com os dois eventos
- resolvido:
  - o molde canônico `claude-harness/hooks/hooks.json` definia o hook **Stop** com
    `type: "prompt"` (Haiku); o Claude Code só aceita `type: prompt`/`agent` em
    eventos de tool (PreToolUse/PostToolUse/PermissionRequest) — em `Stop` ele é
    ignorado silenciosamente. Convertido na fonte para `type: "command"` emitindo
    `systemMessage`, mais nota explicativa no `_comment`
- decisões:
  - hooks vão no **`settings.local.json`** (já coberto pelo gitignore global
    `**/.claude/settings.local.json`) — ficam só na máquina, nada a versionar
  - `matcher: Bash|PowerShell` no PreToolUse porque o WIC é Windows-first
  - Stop aponta explicitamente para a convenção deste diário
    (`.claude/contexto/AAAA-MM-DD.md`)
- próximos:
  - os hooks só ativam ao abrir o Claude Code **dentro de**
    `Windows_Intelligence_Center` (esta sessão rodou com cwd em `IdeaDDoS`); na 1ª
    sessão lá, se não dispararem, abrir `/hooks` uma vez (recarrega a config) ou
    reiniciar
- artefatos:
  - `.claude/settings.local.json` — bloco `hooks` adicionado
  - `../claude-harness/hooks/hooks.json` — Stop hook corrigido + `_comment`
  - `.claude/contexto/2026-06-28.md` (este arquivo)
