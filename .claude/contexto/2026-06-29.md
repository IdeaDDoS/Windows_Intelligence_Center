# 2026-06-29 (segunda-feira)

## 00:36 — Execução autônoma das fatias 1–4 (Fase 1)

- feito:
  - **Fatia 1 (métricas ao vivo)** commitada (`43615ec`): estava pronta e verde,
    mas parada como untracked — destravado o ponto onde o 1º teste do harness havia
    parado (entregou sem commitar)
  - **Fatia 2 (processos/serviços/detalhe)** (`e330a82`): collectors
    `processes`/`services`/`signature`, API `/api/processes(+/{pid})` e
    `/api/services`, navegação react-router + tabelas + modal de detalhe
  - **Fatia 3 (histórico)** (`35dc370`): `metric_samples` (schema v2), `sampler`
    asyncio no lifespan (retenção 7 dias), `/api/metrics/history`, gráfico Recharts
  - **Fatia 4 (alertas)** (`9d3b2ec`): `alert_rules`/`alerts` (schema v3 + seed),
    `AlertEngine` com `duration_s`, API de alertas/regras, badge + lista + config
  - suíte final verde: **backend 32 pytest + frontend 7 vitest**, `tsc --noEmit` limpo
  - PLANEJAMENTO atualizado (roadmap 1–4 ✅ + nota de status)
- resolvido:
  - `collect_services` quebrava com `FileNotFoundError` em `as_dict()` (chama
    `description()`): passou a ler campos individualmente e degradar `partial`
  - erro de tipo no `AlertRulesTable` (patch tipado como `AlertRulePatch`)
- decisões:
  - `MetaPayload` centralizado em `api/common.py` (uma fonte canônica; `metrics.py`
    passou a reusar)
  - handlers que fazem I/O pesado (psutil em massa, PowerShell, SQLite) são `def`
    síncronos → FastAPI os joga no threadpool (sem travar o event loop)
  - assinatura digital chama PowerShell com o caminho via `$env:WIC_SIG_PATH`
    (sem concatenar entrada no comando — evita injeção)
  - `config.sampler_enabled` desliga o sampler nos testes; `tests/conftest.py`
    isola o DB em arquivo temporário por teste
  - navegação via react-router (já estava nas deps); Recharts instalado nesta rodada
- próximos:
  - **push** das 4 fatias fica com o usuário (protocolo: publicação é sempre humana)
  - higiene: `.txt` e `atividade de faculdade/` seguem untracked (não são do produto)
  - alvo natural seguinte: Fatia 5 (Logs / Event Viewer via Get-WinEvent)
- artefatos:
  - commits `43615ec`, `e330a82`, `35dc370`, `9d3b2ec`
  - `docs/PLANEJAMENTO.md` (roadmap + status); este registro

## 00:49 — Fatia 5 (Logs / Event Viewer) — Fase 2

- feito:
  - **Fatia 5** (`4dd889e`): Event Viewer on-demand — collector `events`
    (Get-WinEvent via PowerShell, filtros por env var), `GET /api/events`
    (log/level/since/limit), página com filtros e tabela; nav `/events`
  - push das 6 commits anteriores (fatias 1–4 + docs) feito antes desta fatia
    (`9114412..1114186`)
  - PLANEJAMENTO: roadmap 5 ✅, decisão travada (on-demand, sem tabela `events`)
    e "Detalhe da Fatia 5" pós-fato registrados
  - suíte: **backend 40 pytest + frontend 8 vitest** verdes; `tsc` limpo
- decisões:
  - eventos **on-demand** (escolha do usuário): cada request roda Get-WinEvent;
    sem persistência. A tabela `events` do §5 fica para uma fatia futura de
    correlação (plano é documento vivo)
  - "No events found" do Get-WinEvent = lista vazia, não erro; `Security` sem
    elevação degrada `partial`
- próximos:
  - push da Fatia 5 fica com o usuário
  - alvo seguinte: Fatia 6 (postura de segurança: ports/services/security_config
    + findings + score) — Fase 4
- artefatos:
  - commit `4dd889e`; `docs/PLANEJAMENTO.md` (Detalhe da Fatia 5); este registro

## 01:04 — Fatia 6 (postura de segurança) — Fase 4

- feito:
  - **Fatia 6** (`9153fcb`): auditoria de segurança completa portada do motor de
    referência `security_audit` — collectors `ports`+`security_config`, analyzers
    `findings`+`scoring`, `audits`/`findings` (schema v4), `services/audit.run_audit`,
    `api/audit` (run/latest/histórico/{id}), `SecurityPage` (score+findings+config+portas)
  - push da Fatia 5 feito antes (`1114186..4c55ce4`)
  - PLANEJAMENTO: roadmap 6 ✅, decisão travada e "Detalhe da Fatia 6"
  - suíte: **backend 64 pytest + frontend 10 vitest** verdes; `tsc` limpo
- decisões:
  - escopo **completo** (escolha do usuário): portas + security_config + findings +
    score, persistindo audits/findings; auditoria roda **sob demanda** (POST), com
    histórico
  - findings usam `is False` para não sinalizar dado ausente (coleta parcial sem admin)
  - `security_config` faz **uma** chamada PowerShell agregada (firewall/Defender/AV/
    hotfix) — mais simples de mockar e mais rápido
- próximos:
  - push da Fatia 6 fica com o usuário
  - alvo seguinte: Fatia 7 (explicações com Claude/Haiku + cache) — Fase 3.
    Atenção: 1ª fatia a usar `anthropic` (feature-flag, opcional, `ANTHROPIC_API_KEY`)
- artefatos:
  - commit `9153fcb`; `docs/PLANEJAMENTO.md` (Detalhe da Fatia 6); este registro
